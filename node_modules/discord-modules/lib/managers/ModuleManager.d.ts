import { Collection } from "discord.js";
import { Client } from "../Client";
import { Module } from "../structs/Module";
export declare class ModuleManger {
    readonly client: Client;
    readonly registry: Collection<string, Module>;
    constructor(client: Client);
    /**
     * Register a specific module. Likely used to register module's commands and listeners.
     *
     * @param module Module to be registered.
     * @returns {Promise<void>}
     */
    register(module: Module): Promise<void>;
}
//# sourceMappingURL=ModuleManager.d.ts.map